---
import { i18nParams } from '../../i18n/consts'
import { useTranslations } from '../../i18n/utils';
import Base from '../../layouts/Base.astro';
import HeroHeader from '../../components/HeroHeader.astro';
import Button from '../../components/ui/Button.astro';

export function getStaticPaths() {
	return i18nParams;
}
import { fade } from "astro:transitions";

const { lang } = Astro.params;
const t = useTranslations(lang);
---
<Base>
    <HeroHeader 
        headline={t('hero.headline')}
        text={t('hero.text')}
        buttonText={t('hero.button')}
    />
    <div class="container max-w-screen-xl mx-auto px-8 md:px-0 py-20">
        <div class="flex flex-col md:flex-row items-center justify-center space-y-6 md:space-y-0 md:space-x-10">
            <div class="md:w-1/2">
                <img src="https://via.placeholder.com/600x400" alt="About Us" class="rounded-lg">
            </div>
            <div class="md:w-1/2">
                <h2 class="text-3xl text-primary font-semibold mb-4">{t('about.headline')}</h2>
                <p class="text-lg leading-relaxed">{t('about.text')}</p>
            </div>
        </div>
    </div>

    <div class="bg-gray-100 py-20">
        <div class="container max-w-screen-xl mx-auto px-4 md:px-0  text-center">
            <h2 class="text-3xl font-semibold mb-8">{t('services.headline')}</h2>
            <div transition:animate={fade({ duration: '2s' })} class="grid grid-cols-1 md:grid-cols-3 gap-10">
                <div class="py-3 px-8 w-100 bg-base-100 shadow-2xl rounded">
                    <div class="card-body">
                        <h3 class="title text-xl text-primary font-semibold mb-4">{t('services.item1.headline')}</h3>
                        <p class="text-gray-700">{t('services.item1.text')}.</p>
                        <div class="card-actions mt-2 justify-center">
                            <Button>Read More</Button>
                        </div>
                    </div>
                </div>
                <div class="py-3 px-8 w-100 bg-base-100 shadow-2xl rounded">
                    <div class="card-body">
                        <h3 class="title text-xl text-primary font-semibold mb-4">{t('services.item2.headline')}</h3>
                        <p class="text-gray-700">{t('services.item2.text')}.</p>
                        <div class="card-actions mt-2 justify-center">
                            <Button>Read More</Button>
                        </div>
                    </div>
                </div>
                <div class="py-3 px-8 w-100 bg-base-100 shadow-2xl rounded">
                    <div class="card-body">
                        <h3 class="title text-xl text-primary font-semibold mb-4">{t('services.item3.headline')}</h3>
                        <p class="text-gray-700">{t('services.item3.text')}.</p>
                        <div class="card-actions mt-2 justify-center">
                            <Button>Read More</Button>
                        </div>
                    </div>
                </div>
                    
            </div>
        </div>
    </div>

    <div class="container mx-auto px-8 md:px-0 py-20">
        <div class="text-center">
            <h2 class="text-3xl text-primary font-semibold mb-4">{t('contact.headline')}</h2>
            <p class="text-lg text-gray-700 mb-8">{t('contact.text')}</p>
            <Button>{t('contact.button')}</Button>
        </div>
    </div>

</Base>