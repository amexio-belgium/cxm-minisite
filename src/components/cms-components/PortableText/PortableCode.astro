---
import { Code } from "astro:components";

export type Props = any;

const props = Astro.props;
---

<Code
  theme={props.node?.style === "dark" ? "slack-dark" : "catppuccin-latte"}
  class="rounded-lg py-4"
  code={props.node?.Code.code}
/>
