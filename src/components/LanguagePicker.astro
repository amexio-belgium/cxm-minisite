---
import { languages } from '../i18n/consts';
---
<ul>
  {Object.entries(languages).map(([lang, label]) => (
    <li>
      <a class="text-white" href={`/${lang}/`}>{label}</a>
    </li>
  ))}
</ul>